Machine Learning: Random Forest, preprocessing pipeline, categorical encoding and saved Joblib model.

Backend: FastAPI, validation, CORS, SQLite and authentication.

Frontend: Login/register, dashboard, loan form, prediction confidence, statistics and history.
